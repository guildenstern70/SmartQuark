quarkus:
  log:
    level: INFO
    console:
      enable: true
      format: "%d{HH:mm:ss} %-5p [%c{2.}] %s%e%n"
      level: DEBUG
      color: true
  swagger-ui:
    always-include: true
    theme: "feeling-blue"
    title: "SmartQuark API | Swagger UI"
    urls-primary-name: "SmartQuark API"
  banner:
    path: banner.txt
  datasource:
    db-kind: ${DB_KIND:mariadb}
    username: ${DB_USERNAME:alessio}
    password: ${DB_PASSWORD:qLKmatt07}
    jdbc:
      url: ${DB_URL:jdbc:mariadb://mariadbtestserver077.mariadb.database.azure.com:3306/smqrk?useSSL=true}
  hibernate-orm:
    database:
      # other valid options are: none, create, create, drop, update
      generation: drop-and-create
  native:
    native-image-xmx: 4g
mp:
  openapi:
    scan:
      exclude:
        classes: net.littlelite.smartquark.controller.web.WebController
